# Replay-харнесс prediction (оффлайн-контур)

Измерительный стенд модели v3.1: файлы на входе — файлы/метрики на выходе.
Никаких Redis / Spring / Reactor / прод-пайплайна (закреплено `OfflineContourPurityTest`).

**Нормативный реестр трактовок замороженной спеки** —
`docs/spec-v3.1-addendum-01-interpretations.md` (аддендум 01). Таблицы ниже — навигация
по порогам/тестам стенда, нормативные формулировки НЕ дублируют.

## Как прогнать одной командой

```bash
# весь стенд (формат, метрики, модели, сценарии 15/16 с базлайнами):
./mvnw test -Dtest='biz.ugur.busroutebackend.replay.**'

# только сценарии (печатают базлайны и хэши):
./mvnw test -Dtest=ReplayScenariosTest
```

## Экспорт геометрии из dev-БД (read-only, по требованию)

```bash
./mvnw test -Dtest=GeometryFixtureExporterTest \
  -Dgeometry.export=true -Dgeometry.pass=$DB_PASSWORD \
  -Dgeometry.routes=25,10,8
# → src/test/resources/fixtures/geometry/route-<N>-dir<D>.json
```

## Состав

| Компонент | Роль |
|---|---|
| `GpsFix` + `GpsFixJsonl` | формат фикса = JSONL рекордера (вкл. hdop/satellites/accuracy, wallClock); ридер источник-агностичен (рекордер / синтетика / будущие генераторы) |
| `GeometryFixture` (+exporter) | полилиния 4326 + cumDist[] + L по контракту s-слоя (s = fraction·L, кумулятивный Haversine); сверка с БД: ±0.4 м к `total_distance_*` |
| `PredictionModel` | порт: фикс+геометрия → Estimate(s, speed, mode, varianceS) |
| `models/GeometricSnapModel` | референс: ближайшая точка → s (без фильтра) |
| `models/HoldLastModel` | референс-negative: держит первую оценку |
| `metrics/PositionMetrics` | слой 1: |s−s_true|, шаги, телепорты, смены режима |
| `metrics/ConsistencyMetrics` | слой 2: NEES/NIS + χ²-интервал (покрыто тестами на распределениях) |
| `metrics/ArrivalDetector` | слой 3 (каркас): факт прибытия по П-2 (arr_zone=50 м, arr_speed=5 км/ч — конфиг) |
| `synth/SyntheticScenario` | Scenario-16 (стоянка→разгон 0→V→крейсер), Scenario-15 (крейсер + forward-биас снапа); шум σ, Tugdk-качество, seed, truth-файл |
| `ReplayHarness` | прогон: фиксы+истина+модель → метрики + SHA-256 вывода (детерминизм) |
| `InvariantAssertions` | INV-2/3/7/8 + bounded-error (ядро Scenario-15) |
| `scenarios/ReplayScenariosTest` | живые 15/16 на референсах (+ санити нулевого шума, детерминизм, негативный контроль); каркас 01–14 `@Disabled` |

## Профили сценариев — параметры стенда v1 (утверждены ревью шага 1)

| Параметр | Значение |
|---|---|
| V крейсера | 12.5 м/с (45 км/ч) |
| a разгона | 1.0 м/с² (≤ a_max §4.5) |
| Интервал фикса | 7 с (замер фида: 5–10 с) |
| σ позиции | 5 м |
| SC15-рампа | drift_rate = 0.7 м/фикс (45° вдоль+поперёк), до ≈2×D_max; **SC15b** — константный биас 20 м (доп-профиль) |
| SC16 | стоянка 120 с → разгон 0→V → крейсер; 900 с |
| SC01 | чистый крейсер |
| SC02 | пропуски GPS 30/60/120/300 с |
| SC08 | стоячий борт + шум σ |

## Трактовки порогов стенда v1 (единая таблица; ссылки на тесты)

| Порог/правило | Трактовка | Тест |
|---|---|---|
| SC16 «установившийся ≤ 10 м» | **p95** по t>400 (max одиночного сэмпла при σ=5 нефальсифицируем) | `CoreScenariosTest.sc16DeparturePeakAndSteadyThresholds` |
| SC02 шаги в gap | вне ре-привязки: ≤ ход + R_max-серия; **шаг ветки A (re-init §6) исключён** (сэмпл с mode=RECOVERING) | `sc02GapsNoJumpBeyondRmaxSeriesAndFreezeAfterTmax` |
| Ре-привязка, ветка A (§6) | ТОЛЬКО после freeze (NO_GPS) при \|z_snap−x⁻\|>D_reanchor=200 м → мгновенный re-init через P_init | там же (300с-gap → RECOVERING; 60с-gap → без re-init) |
| Ре-привязка, ветка B (живой N_persist) | БЕЗ шага-разрыва: стягивание сериями R_max×recoveryPullFactor(=4) до сведения | `sc15RampAbsoluteDeviationControlledAndDetected` (assert «стягивание без шага-разрыва») |
| NEES | **сырой одномерный позиционный**: err_s²/P₀₀, dim=1, ожидание 1.0 (НЕ dim=2 ANEES) | `sc16HonestNeesMonteCarloWithinChi2` (печатает формулу, q/R и профиль по времени) |
| «Проезд вставшего» в gap>T_max | наблюдаемо через ветку A: сам факт re-init = прогноз был заморожен и отстал >D_reanchor | `sc02...` |
| N_chatter (SC09) | **метрика дребезга = повторные входы DECEL_ENTER к одному стопу за визит (норма 0)**; сырое число смен mode — диагностическая печать, не критерий (ратификация A4) | `StopEtaScenariosTest.sc09SawNoModeChatter` |
| Headline на синтетике | верификация **механики** стенда; реальная приёмка модели — replay живых логов после кампании записи (ратификация A4) | `headlineEtaVsFactByHorizon` |

## Отступления defaults() от Приложения спеки (явная секция)

| Константа | Спека | Стенд v1 | Причина |
|---|---|---|---|
| `vTargetMs=12.5` | нет (история сегментов — инкремент 2) | 12.5 | профиль v1 |
| `recoveryPullFactor=4` | нет | 4 | скорость стягивания ветки B |
| `sigmaMeasDefaultMeters=15` | R0 не задан | 15 | грубый дефолт при null-качестве |
| остальное (D_snap=80, D_max=120, γ=3, N_persist=4, M=2, T_lost=15, T_max=90, W0=150, a_dep=1.0, a_max=1.5, v_max=22, D_reanchor=200) | диапазоны Приложения | в диапазонах | — |

## Стоп-слой + ETA v1 (шаг A4)

- Фикстуры несут `stops[]` (id, seq, s из свежего снапа; 25/10/8 — 0 инверсий).
- События (§8): DECEL_ENTER / DWELL_ENTER / DWELL_EXIT / SKIP / AT_TERMINAL / DWELL_OUTLIER —
  интерфейс `StopAware` (`etas()`, `drainEvents()`); TURNING/NEW_TRIP — UNIMPLEMENTED (инкремент 3).
- DWELL пинит `x = x_stop`; вход `|x−x_stop|≤ε_stop ∧ raw<v_stop` ≥2 фикса; выход по
  `dwell_expected`(конфиг, 20 с) ∨ `raw≥v_move`; SKIP — по принятому снапу z_x (прогресс),
  событие только если min raw-скорость в зоне ≥ v_move.
- DECEL-профиль §4.3 капит прогнозную v ТОЛЬКО между фиксами (τ>2·dt) И при подтверждённом
  замедлении (последний raw < 0.95·V) — измерения правят прогноз.
- **ETA v1 (INV-11):** из того же x̂; кинематика (разгон a/крейсер/торможение) на целевой стоп от
  текущей v̂ и на сегменты стоп-стоп; стоянка стопа учитывается ПОСЛЕ его собственного ETA;
  в DWELL — остаток dwell; reliable=false в RECOVERING/GPS_LOST/NO_GPS.
- **Пороги A4 (стенд v1):** headline p95|ETA−факт| ≤ 15/25/60 с на горизонтах ≤60/≤120/≤300 с
  (факт: **13.2 / 14.5 / 13.1 с**, мультистоп с dwell-джиттером ±30% и σ=5);
  детектор П-2 vs истина ≤10 с; дрейф в DWELL ≤ ε_dwell=15 м; SC09 — «предельный цикл» =
  повторный DECEL_ENTER к одному стопу (=0), полное число смен mode — печатается.

## Терминалы/флип + история (шаг A5)

**Топология (Р-5/INV-15):** фикстура несёт `topology ∈ {there-and-back, loop}`
(дефолт there-and-back; `branching` — honest-заглушка, `RouteTopology.of` печатает и бросает).
Композит `RouteTopology.thereAndBack(dir0, dir1)` даёт ядру оба плеча; loop — одно кольцо.
Правила: wrap только loop (x←x−L, без смены d, без сброса P; счётчик оборотов `lapCount()`);
флип d и кламп `x=L_d → AT_TERMINAL` — только there-and-back.

**Терминальная цепочка:** вход AT_TERMINAL при `x ≥ L_d − ε_term`; стопы, чья зона пересекает
ε_term (включая `s' ≤ ε_term` обратного плеча), принадлежат терминальной ветке — стоп-слой их
не обслуживает (дубля ARRIVAL+TERMINAL нет). В AT_TERMINAL метка пинится на `x = L_d`.
Признак разворота — реальное продвижение (дух INV-9): TURNING только при
`N=3` подряд принятых снапах на L_{d'} с монотонным ростом и Σ ≥ `D_turn_confirm=40 м`
(близость без продвижения — не признак). TURNING держит последнюю точку; возврат — `K=3`
принятых снапа обратно на L_d. NEW_TRIP: `x = z_x'` ПЕРВОГО подтверждённого снапа
(минимизирует |Δp| цепочки), `P = P_init`, `trip_id++` ровно раз, → TRACKING по первой
штатной привязке. Факт |Δp|: route 10 — 0.0–3.7 м; route 8 — ~70 м (конечные плеч
физически разнесены на ~77 м — порог там калибровочный 150 м).

**Найдено в TDD A5 (алиасинг зоны):** жёсткое торможение в стоп пересекает DECEL-зону [0, ε_arr]
между 7-с фиксами (x̂ с крейсерской v̂ перепрыгивает зону, gate режет коррекции, прогресс
«проезжает» стоп). Лечение в духе A4: **вход в DECEL и учёт min-скорости зоны — также по
принятому снапу z_x**, не только по x̂ (измерения правят прогноз). Побочный эффект:
headline A4 улучшился до 7.8/14.5/12.5 с.

**История сегментов/dwell (батч-вход D, §0.5):** `HistoryAccumulator` (оффлайн-джоб) строит
из прогонов по П-2-выходам (не по истине): `hist_dwell[stop_id] → {mean, n}`,
`hist_seg[from|to|hour_bin|weekend] → {mean, n}`; выбросы `dwell > dwell_max` не входят.
Подключение (read-only `SegmentDwellHistory`, ядро писателя не видит — purity-тест):
`dwell_expected(stop)` = история при `n ≥ N_min=5`, иначе константа 20 с;
`t_seg` полных легов ETA = история при совпадении hour_bin, иначе кинематика;
`v_target` сегмента для §4.1/§6 — из истории при наличии. Первый лег (x̂ → ближайший стоп) —
всегда кинематика. Приёмка: dwell 10/20/45 с — история p95 17.6/18.0 с против константы
28.9/28.9 с (те же сиды); OOD-стоп → fallback в порогах; выброс 900 с в историю не попал.

**Honest-границы v1 (A5.5):** ETA через разворот не считается (только остановки впереди на
текущей линии); OFF_ROUTE — заглушка (FSM 11/12); branching вне scope.

**Baseline-артефакт:** `reports/eta-baseline-null-model.md` (нулевая модель Δs/v_target vs ядро,
генерируется `EtaBaselineReportTest`).

## OFF_ROUTE (шаг A6, Scenario-06) — FSM 12/12

Нормативные формулировки — аддендум 01, №15–18. Кратко: вход по K=5 подряд отказов коридора
(D_off=D_snap); внутри — x̂ заморожен, P растёт, ETA reliable=false, события стоп-слоя не
эмитятся; возврат по 1 валидному фиксу — вливание (gap ≤ D_reanchor) или ре-привязка веткой A;
эскалация INV-3 разведена с INV-4 (persist→RECOVERING только при валидном снапе).
Тесты: `OffRouteScenariosTest` (вход ровно после K, заморозка, оба пути возврата, флап-метрика,
negative OFF_ROUTE→DWELL), `turningGpsLossFallsToGpsLostWithoutFalseNewTrip` (A6.6).
Флаги (не чинилось, вопросы к спеке): флап на касаниях коридора (4 перехода/объезд при
2 касаниях — возврат по букве «1 фикс»); потеря флипа после gap посреди TURNING, если борт
ушёл за окно wTurnWindow (кандидат A7). Заглушек режимов не осталось — все 12 состояний FSM живые.

## Банк гипотез Р-7 + дебаунс OFF_ROUTE (шаг A7)

Нормативная база — аддендум-02 (варианты) и аддендум-01 №19–23. Кратко:
- **Банк** (`HypothesisBank`): гипотеза = вариант геометрии + лёгкий alpha-beta-трек + EMA-скор;
  дефолт {d, d'}, варианты через `RouteTopology.withVariants` (справочник — данные, круг 6 + V97);
  лидер вещается, смена с гистерезисом; смена со сменой d → RECOVERING→NEW_TRIP→TRACKING;
  вход OFF_ROUTE = K отказов на ВСЕХ гипотезах. Быстрый терминальный флип A5 сохранён — банк
  страхует вне конечной (missedFlip: перехват за ~22 фикса, факт печатается).
- **Дебаунс выхода OFF_ROUTE** (№21): M=2 подряд принятых снапа с согласованным продвижением;
  касание коридора не размораживает x̂ (флап-профиль: 0 ложных выходов, переходов ≤ 2).
- **τ/conf (A7.0, №20):** τ_valid сбрасывается любым фиксом (факт кода); деградация уверенности
  в серии отказов — P-ростом (ассерт монотонности в `a70Confidence...`).
- Тесты: `VariantBankScenariosTest` (SC07 восьмёрка, SC14 ранняя/поздняя дизамбигуация,
  missedFlip, стабильность банка — headline 7.8/14.5/12.5 без сдвига при 0 смен,
  оверхед 24.5→53.7 мкс/фикс при N_hyp 2→6).

**Honest-границы v1 (ратификация A7):**
- **M_flip ≈ 22 фикса (~150 с)** на потерянном флипе — v1-граница краевого случая;
  калибровка скоринга — ТОЛЬКО по живым логам (A9), порядок ручек: λ, c_rej → H_sw последним.
- **Стоячий возврат из OFF_ROUTE** — уточнено фактом A8.4: борт, стоящий В коридоре,
  выходит по шумовому знаковому критерию за ~2–5 фиксов (задержка, не запирание);
  строгое запирание дебаунса действует для бортов ВНЕ коридора (d_snap > D_snap).
  Каскад «объезд→конечная→отъезд d′»: выход из OFF за ~6 тиков → штатный терминальный
  флип, NEW_TRIP через ~17 с после физического разворота, банк не потребовался.

## Контракт §14 + replay-конвейер (шаг A8)

**Исполняемый контракт** — `scenarios/SpecScenarioContractTest`: 16/16 именованных тестов
с порогами §14 и печатью фактов. Маппинг на глубокие пакеты:

| SC | Контракт-тест | Глубокое покрытие |
|---|---|---|
| 01 идеал | новый | — |
| 02 gaps | новый (тонкий) | `CoreScenariosTest.sc02...` |
| 03 loop | новый (тонкий) | `TerminalTopologyScenariosTest.sc03...` |
| 04 разворот | новый (тонкий) | `...sc04TurnFlipChain...` |
| 05 овершут | **новый** | — |
| 06 off-route | новый (тонкий) | `OffRouteScenariosTest` |
| 07 восьмёрка | новый (тонкий) | `VariantBankScenariosTest.sc07` |
| 08 стоянка | **новый** (нашёл баг DWELL_EXIT) | — |
| 09 пила | новый (тонкий) | `StopEtaScenariosTest.sc09` |
| 10 accuracy | **новый** (Р-8) | — |
| 11 телепорт | **новый** | — |
| 12 валидация | **новый** (`InputValidator` §9) | — |
| 13 тоннель | **новый** | — |
| 14 варианты | новый + fork_gap | `VariantBankScenariosTest.sc14...` |
| 15 рампа | новый (тонкий) | `CoreScenariosTest.sc15...` |
| 16 отрыв | новый (тонкий) | `CoreScenariosTest.sc16...` |

**Р-8/INV-14 (аудит A8.0):** R = f(d_snap, z_acc) реализован в `measurementSigma`
(base = max(accRef, z_acc) либо hdop-производная, ×(1+d_snap/D_snap)); форма отличается
от буквы §5.2 ((1+x)² vs (1+x²)) — монотонность эквивалентна, ассерты в SC10
(K-вклад инновации 35 м: acc=3 → 34.7 м; acc=40 → 22.6 м; полная монотонность по сетке).
Отсутствие z_acc → дефолт σ=15 (поведение до-A8, регрессия 0).

**Найден и закрыт баг ядра (SC08):** DWELL_EXIT по `expired` без движения уводил стоящий
борт в DEPARTING (CA-профиль разгонял x̂ — дрейф 414 м за 10 мин стоянки). Выход из DWELL —
только по движению (§8 «позиция заморожена»); DWELL_OUTLIER остаётся диагностикой.

**Конвейер корпуса** (`pipeline/`): `CorpusLoader` (JSONL → эпизоды: борт × непрерывный
отрезок, разрыв по gap>T_max/смене маршрута) → `EpisodeReplayRunner` (валидация §9, ядро,
метрики: ETA p95 по горизонтам с П-2-фактом, события, ре-привязки, смены лидера, доли
OFF/LOST, |ν|, NIS) → `CorpusReplayReport` (markdown + SHA). Мини-корпус: 5 эпизодов,
отчёт `reports/replay-mini-corpus.md`. **Живой корпус одной командой:**
`./mvnw test -Dtest=CorpusReplayTest -Dcorpus.dir=<путь>` (геометрии новых маршрутов —
экспортёром в фикстуры + строка в `GEOMETRY_BY_ROUTE`).

## Пороги стенда v1 (калибруемые, не спека)

- SC16: пик на отрыве ≤ 25 м; установившийся ≤ 10 м — **трактовка: p95 по t>400**
  (одиночные шумовые сэмплы при σ=5 дают хвосты >10; max нефальсифицируем стабильно).
- SC02: шаг вне ре-привязки ≤ ход + R_max-серия; шаг самой ре-привязки (mode
  RECOVERING→TRACKING) — санкционированный спекой §5.5 скачок, из allowance исключён;
  проезд «вставшего» в gap ≤ v_target×T_max×1.25.
- SC08: дрейф ≤ 15 м. Р-2: 0 RECOVERING на гладком профиле.
- Конфиг констант ядра: `core/CoreConfig.defaults()` (дефолты Приложения спеки).

## NIS / NIS*

У референс-моделей (без предсказания) колонка — **NIS\*** (суррогат: шаг оценки между
фиксами). У ядра (`MotionFilterCore`, интерфейс `InnovationAware`) — **настоящий NIS**:
ν = z_x − x⁻, S = P₀₀ + R.

## SC15: почему «ошибка к истине» — НЕ критерий (обязательное пояснение)

На рампе SC15 измерения систематически смещены — истина недостижима ЛЮБОЙ каузальной
моделью (следящий снап честно уезжает вместе с измерениями: базлайн early≈24 м →
late≈86 м, линейный рост — портрет Н-1). Критерий Н-1 для ядра:
**ограниченность абсолютного отклонения** |Γ(x̂) − raw| против D_max (INV-3, независимо
от пер-тиковых дельт) + **детекция** устойчивого расхождения (N_persist → RECOVERING →
ре-привязка ≤ M подтверждений). Ассерт: ни одного тика, где absDev > D_max и событие
дрейфа не активно. С шага A6 «активное событие» включает OFF_ROUTE (аддендум №18):
поздняя фаза рампы (перп-смещение > D_snap) легитимно уходит из коридора — метка
замирает с флагом вместо выдумывания прогресса; редкие валидные снапы на изгибах
возвращают веткой A (санкционированные §6-скачки исключены из step-ассерта, как в SC02).
